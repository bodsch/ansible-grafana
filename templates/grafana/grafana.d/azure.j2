
[azure]
{% if grafana_azure.cloud is defined and
      grafana_azure.cloud | string | length > 0 and
      grafana_azure.cloud in ["AzureCloud", "AzureChinaCloud", "AzureUSGovernment", "AzureGermanCloud"] %}
cloud = {{ grafana_azure.cloud }}

  {% if grafana_azure.managed_identity_enabled is defined %}
managed_identity_enabled = {{ grafana_azure.managed_identity_enabled | bool | ternary('true', 'false') }}
  {% endif %}
  {% if grafana_azure.managed_identity_client_id is defined and
        grafana_azure.managed_identity_client_id | string | length > 0 %}
managed_identity_client_id = {{ grafana_azure.managed_identity_client_id }}
  {% endif %}
{% endif %}
